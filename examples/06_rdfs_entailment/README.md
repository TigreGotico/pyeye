# 06 — RDFS Entailment

**Topic:** Class hierarchies
**Key concepts:** `entail=True`, `rdfs:subClassOf`, domain/range

entail=True activates RDFS reasoning: class hierarchies, property hierarchies, domain/range constraints, and type propagation.

Run:

```bash
python rdfs_demo.py
```

Back to the [examples index](../README.md).
