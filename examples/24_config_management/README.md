# 24 — Configuration Management

**Topic:** Layered configuration
**Key concepts:** `log:onNegativeSurface` priority, config validation

A practical use case: managing layered configuration with defaults, overrides, and validation using N3 reasoning.

Run:

```bash
python config_demo.py
```

Back to the [examples index](../README.md).
