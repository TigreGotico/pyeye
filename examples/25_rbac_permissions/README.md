# 25 — Role-Based Access Control (RBAC)

**Topic:** Role-based access control
**Key concepts:** Role hierarchy, permission inheritance, deny override

A full RBAC policy engine implemented in N3: roles, permissions, inheritance, and dynamic access decisions — all as pure inference rules.

Run:

```bash
python rbac_demo.py
```

Back to the [examples index](../README.md).
