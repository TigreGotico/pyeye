# 01 — Hello World

**Topic:** First inference
**Key concepts:** `execute()`, `data_strings`, `rule_strings`, `=>`

The simplest possible inference: one fact, one rule, one derived fact.

Files: `hello.n3`

Run:

```bash
python hello.py
```

Back to the [examples index](../README.md).
