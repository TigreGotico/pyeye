# 03 — Math Builtins

**Topic:** Arithmetic & comparisons
**Key concepts:** `math:product`, `math:greaterThan`, list-arg syntax

Use the math: namespace to do arithmetic inside rules.

Run:

```bash
python math_demo.py
```

Back to the [examples index](../README.md).
