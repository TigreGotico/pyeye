# 18 — Skolem & UUID: Generating Identifiers

**Topic:** Identifier generation
**Key concepts:** `log:skolem` (deterministic), UUID via Python

When rules need to create new resources (not just assert properties of existing ones), they need fresh, unique identifiers.

Run:

```bash
python skolem_demo.py
```

Back to the [examples index](../README.md).
