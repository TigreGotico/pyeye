# 19 — Math Statistics

**Topic:** Aggregate statistics
**Key concepts:** `log:collectAllIn` + `math:` for avg/min/max

Combine log:collectAllIn with math: aggregation builtins to compute summary statistics over sets of values derived from the knowledge graph.

Run:

```bash
python stats_demo.py
```

Back to the [examples index](../README.md).
