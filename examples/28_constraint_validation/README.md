# 28 — Constraint Validation (SHACL-like)

**Topic:** SHACL-like constraint validation
**Key concepts:** Required properties, cardinality, value-range constraints, violation reports

Validate RDF data against shape constraints and produce structured violation reports — similar to SHACL (Shapes Constraint Language) but implemented purely in N3 rules.

Run:

```bash
python shacl_demo.py
```

Back to the [examples index](../README.md).
