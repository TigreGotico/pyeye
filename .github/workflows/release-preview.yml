name: Release Preview

on:
  pull_request:
    branches: [dev]
  workflow_dispatch:

jobs:
  release_preview:
    uses: OpenVoiceOS/gh-automations/.github/workflows/release-preview.yml@e37a97650feada694abb6420a84bbd23a71d6bb1
    secrets: inherit
    with:
      package_name: 'pyeye'
      version_file: 'pyeye/version.py'
