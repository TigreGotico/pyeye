name: Repo Health

on:
  pull_request:
    branches: [dev, master, main]
  workflow_dispatch:

jobs:
  repo_health:
    uses: OpenVoiceOS/gh-automations/.github/workflows/repo-health.yml@e37a97650feada694abb6420a84bbd23a71d6bb1
    secrets: inherit
    with:
      version_file: 'pyeye/version.py'
